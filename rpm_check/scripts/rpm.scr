/**
 * File:
 *   rpm.scr
 * Summary:
 *   SCR Agent for working with Red Hat Package Manager (rpm)
 *   and creating/reading "rpm difference Data Base" (rpm_dDB)
 *
 * Access:
 *   read-write
 * Authors:
 *   Maksym Borovytskiy <maxxx@suse.de>
 * See:
 *   libscr
 *   man rpm
 *   rpm_check docs
 *   rpm_check sources
 **
 *
 * $Id$
 *
 * <p>agent for verifying of rpm-packages and manage 
 * rpm_dDB (rpm difference Data Base)</p>
 */
.rpm

`ag_rpm ()
